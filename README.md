# 画像ビューア（仮称）

サムネイル一覧で画像を眺めながら、選択した画像（複数可）にメニューやショートカットキーから
リサイズ・切り抜き・連結などの編集を行う Windows 用画像ビューア。

[image-sizechange](https://github.com/HatoAttack/image-sizechange) の後継として開発中。

## 構成

| プロジェクト | 役割 |
|---|---|
| `src/ImageViewer.Core` | UI に依存しない中核（画像読み込み・対応形式・コマンドの仕組み・編集処理） |
| `src/ImageViewer.App` | WinForms の UI（ツールバー・一覧・1枚表示・メニュー・ダイアログ・配色） |
| `tests/ImageViewer.Tests` | 動作テスト（`dotnet run --project tests/ImageViewer.Tests`） |

.NET 8 + WinForms + ImageSharp。

## コマンドの仕組み

編集機能はすべて `IImageCommand`（`src/ImageViewer.Core/Commands`）として実装し、`MainForm.RegisterCommands` で登録する。
☰ メニュー・右クリックメニュー・ショートカットは登録済みコマンドから自動で組み立てられ、
選択枚数に応じて実行可否（例: 連結は2枚以上、切り抜きは1枚）が切り替わる。

```csharp
public sealed class CopyPathsCommand : ImageCommandBase
{
    public override string Id => "file.copyPaths";
    public override string Name => "パスをコピー";
    public override string Category => "ファイル";          // メニューのグループ
    public override string? DefaultShortcut => "Ctrl+Shift+C";
    protected override int MinSelection => 1;              // 選択枚数の条件
    public override Task ExecuteAsync(CommandContext context) { ... }
}
```

## 設定・保存データ

`%LOCALAPPDATA%\ima-ge-viewer\` に保存する。どれも小さく上限があり、壊れていたら初期値で動く（起動できなくならない）。

| ファイル | 内容 |
|---|---|
| `settings.json` | 設定（ホームフォルダ・サムネイルの大きさ・配色・サイドバーと詳細パネルの表示・フォルダジャンプ・リサイズと連結で前回使った設定） |
| `orders\*.json` | 手動の並び順（フォルダごと、最大 1000 件） |
| `folder-index.bin` | フォルダジャンプの索引（フォルダ名と親の番号だけ。最大 30 万件、ユーザーフォルダで数十 KB〜数 MB） |
| `visits.json` | 開いたフォルダの記録（最大 500 件） |

書き込みは一時ファイルに書いてから差し替えるので、途中で落ちても壊れたファイルは残らない。

## 対応形式

| 区分 | 形式 |
|---|---|
| どの PC でも読める（ImageSharp） | JPG / PNG / GIF / WEBP / BMP / TIFF / TGA / ICO / CUR / PBM 系 / QOI |
| Windows の拡張機能があれば読める（WIC） | HEIC / HEIF / AVIF / JPEG XL / 各社 RAW / JPEG XR / DDS など |

WIC の対応形式は起動時にその PC のデコーダを列挙して決まる（☰ →「ヘルプ → 対応形式」で確認できる）。
HEIC は「HEIF 画像拡張機能」＋「HEVC ビデオ拡張機能」、AVIF は「AV1 Video Extension」、RAW は「Raw Image Extension」が必要。

## 軽さの工夫

- サムネイルは「表示中 → 下に 1 画面 → 上に 1 画面」の分だけ要求し、スクロールで外れた要求は捨てる
- まずエクスプローラーと同じシェルのサムネイル（thumbcache）を使い、取れないときだけ自前で縮小読み込み
- メモリ上のサムネイルは合計 128MB を上限に古いものから破棄（1500 枚のフォルダを端までスクロールしてもプロセス全体で約 150MB）
- 縮小デコードが効かない形式（PNG / WEBP 等）は同時に 2 枚までしか展開しない

## 現状と予定

- [x] フォルダを開く（Ctrl+O・フォルダのドロップ・起動引数）
- [x] コマンドの仕組み（メニュー / 右クリック / ショートカット、複数選択）
- [x] 読み込み層 `ImageLoader`（ImageSharp の縮小デコード + WIC 経由で HEIC / AVIF / RAW / JPEG XL をベストエフォート対応、回転補正・sRGB 変換）
- [x] サムネイル生成 `ThumbnailService`（シェルのサムネイル → 縮小デコード、表示中優先の待ち行列、上限付きメモリキャッシュ）
- [x] サムネイルグリッド（見えている分＋前後 1 画面だけ生成、クリック / Ctrl / Shift / キー操作 / ドラッグ範囲選択で複数選択）
- [x] チェック（¥ で付け外し（複数選択ならまとめて）、^ で付け外しして次へ、Shift+¥ で選択中にチェック、Ctrl+¥ でチェックした画像を選択。¥ / ^ は settings.json の MarkKey / MarkNextKey で変更可）
- [x] Quick Look: Space（短く押すと開いたまま / 押し続けると離したら閉じる）・ダブルクリックで大きく表示。表示中も ← → ¥ ^ が使える
- [x] 並び順（名前・更新日時・サイズ・手動）。手動はドラッグで並べ替え、フォルダごとに保存（`%LOCALAPPDATA%\ima-ge-viewer\orders`）
- [x] 名前の変更（F2）: 文字列＋連番 / 置換・末尾追加 / 小文字化。プレビュー付き、2 段階で実行して失敗時は全部元に戻す、Ctrl+Z で元に戻す
- [x] ファイラとしての移動: アドレスバー（Ctrl+L、補完あり）・戻る / 進む / 上へ（Alt+← / Alt+→ / Alt+↑・Backspace・マウスの戻る / 進むボタン）・フォルダツリー・グリッドにサブフォルダを表示
- [x] サムネイルの大きさ: フッターのスライダー / Ctrl+ホイール（64〜320。作る大きさは 4 段階で、段階をまたがなければ作り直さない）
- [x] 新しいフォルダー（Ctrl+N / 右クリック）
- [x] フォルダー名の変更（フォルダのタイルを 1 つ選んで F2 / ファイルメニュー / 右クリック。画像を選んでいるときの F2 は画像の名前の変更）
- [x] 更新の確認: 起動時に 1 回だけ GitHub の最新リリースを確認し、新しければフッターと ☰ のヘルプに表示（作業は止めない）。
      クリックで変更内容を見て「更新する / 後で / このバージョンは飛ばす」。更新はチェックサム（SHA-256）を確かめてから exe を入れ替えて再起動。
      ☰ のヘルプで起動時の確認を止められる（「更新を確認」で手動でも確認できる）。開発用のビルドでは起動時に確認しない
- [x] 削除（Del / ファイルメニュー / 右クリック）: 選択中の画像をごみ箱へ移動（ごみ箱から元に戻せる。確認は複数枚のときだけ）。削除後は次の画像を選択、Quick Look 中なら次の画像を表示
- [x] コピー / 切り取り / 貼り付け（Ctrl+C / Ctrl+X / Ctrl+V・編集メニュー・右クリック）: エクスプローラーと同じクリップボード形式なので、エクスプローラーとの間でも貼り付けられる。同じフォルダへのコピーは「- コピー」を付けて複製、同じ名前があれば Windows の上書きの確認。貼り付けたものを選択
- [x] ドラッグ＆ドロップで移動 / コピー: 画像をフォルダのタイル・ツリーのフォルダに落とす（エクスプローラーからも）。
      エクスプローラーと同じく、同じドライブなら移動・別のドライブならコピー、Ctrl でコピー・Shift で移動。ツリーは止めておくと開く。
      エクスプローラー等へのドラッグも同じ規則で、移動された画像は一覧から外れる
- [x] フォルダーへ移動（Ctrl+Shift+M）/ フォルダーへコピー（Ctrl+Shift+D）: 移動先はフォルダジャンプと同じ検索で選ぶ。入力が空なら最近の移動先（最大 10 件）
- [x] フッターに選択中の画像の情報（1 枚: 解像度・形式・ファイルサイズ・更新日時 / 複数: 枚数と合計サイズ）
- [x] ホームフォルダ: Alt+Home / ☰ の移動で移動、起動時に開く、ツリーの先頭に表示（☰ の移動で設定。未設定ならピクチャ）
- [x] Listary 風のフォルダジャンプ: アドレスバーにフォルダ名の一部を入力すると候補を表示（Ctrl+J で空から入力）。
      スペース区切りで絞り込み（`2024 旅行`）、あいまい一致（`imgszch` → `image-sizechange`）、よく開く・最近開いたフォルダを上位に。
      自前の軽い索引（ユーザーフォルダ＋追加したフォルダ、最大 30 万件）。Everything との連携は設定で選んだ人だけ（同梱はしない）
- [ ] UI の作り直し（v1.0 に向けて）。設計は [docs/ui-concept.md](docs/ui-concept.md)
  - [x] ツールバー 1 本化（☰ メニュー・サイドバーの表示 / 非表示・戻る / 進む / 上へ・アドレスバー・並び順）。Alt / F10 で ☰ を開く
  - [x] 高さ固定のフッター（件数・チェックの数・選択中の画像の情報・サムネイルの大きさ）
  - [x] 配色: システムに合わせる / ライト / ダーク（☰ → 表示 → テーマ。フッター右端のボタンでライト ↔ ダーク）。ダイアログはまだライトのまま
  - [x] フッターの操作ボタン: 画像を選んでいる間はフッターが同じ高さのまま操作ボタンに変わる（対象の切り替え［選択 / チェック］・リサイズ・切り抜き・連結・名前・移動 ▾・⋯・削除・選択を解除 Esc）。
        移動 ▾ は最近の移動先を 1〜9 のキーで選べる（Ctrl を押しながらでコピー）。幅が足りないときはショートカットの表示 → 文字の順に省く
  - [ ] リサイズ ▾（前回の設定のまま実行）
  - [x] 詳細パネル: 一覧の右側（ⓘ / Ctrl+I で開け閉め）と 1 枚表示の右側（I キー）。ファイル名・大きさ・形式・ファイルサイズ・更新日時と、
        撮影情報（撮影日時・カメラ・レンズ・F 値・シャッター速度・ISO。HEIC・RAW など Windows の拡張機能で読む形式は撮影日時とカメラだけ）・場所。
        1 枚表示の間は一覧の右のパネルを隠して画像を広く見せる
  - [ ] アドレスバーからのコマンド
  - [ ] ダイアログのダーク対応
- [ ] 一覧がそのまま拡大表示に変わる 1 枚表示（フィルムストリップ・拡大 / 移動・GIF / WEBP アニメ再生）
- [x] image-sizechange からリサイズ・形式変換・切り抜き・連結を移植（下記）。読み込みはビューアと同じなので HEIC / AVIF / RAW なども元にできる

## 画像の編集（選択中の画像に対して。右クリックメニュー・☰ の「画像」からも）

チェックした画像は Ctrl+¥ で選択してから実行する。複数の画像は画面の並び順（手動で並べ替えた順）に処理する。

### リサイズ・形式変換（Ctrl+R）

| 項目 | 内容 |
|---|---|
| サイズ | 長辺 1600 / 1200 / 600 / 560 px・指定 px・変えない（形式の変換だけ）。既定では小さい画像は拡大しない |
| 形式 | 元のまま / JPG / PNG / WEBP（JPG は透過部分を白に。HEIC・RAW など書き出せない形式の「元のまま」は JPG） |
| 縮小の方法 | Bilinear / Bicubic / Lanczos |
| メタデータ | EXIF・XMP・IPTC を消す（既定。色の情報 ICC は残す）。回転は反映してから消すので向きは崩れない。HEIC・AVIF・RAW など Windows の拡張機能で読む形式は、残す設定でもメタデータを残せない（一覧に表示） |
| ファイル名 | 置換 → 末尾に付ける → 小文字化 |
| 出力先 | 中のフォルダ（既定 `resized`）/ 同じフォルダ / 指定のフォルダ。同名のファイルは飛ばす / 上書き |

- 実行前に「元の画像 → 出力」を一覧で確認できる。名前に \ などを含んで出力先の外に書いてしまうものはエラー。出力の名前が重なるもの（`a.jpg` と `a.png` を両方 PNG に など）はエラーとして示し、直すまで実行できない
- 元の画像を置き換えることになる設定（同じフォルダ・同じ名前・上書き）は確認してから実行する
- 保存は一時ファイルに書いてから差し替えるので、失敗・中断しても書きかけのファイルや壊れた元画像は残らない
- アニメーション（GIF / WEBP）は先頭のフレームだけの静止画になる。WEBP は縦横 16383px まで。JPG / WEBP は品質 90
- 設定は次回の初期値として覚える

### 切り抜き（Ctrl+K）

- 画像の上をドラッグで範囲を選ぶ。枠の中のドラッグで移動、四隅で大きさを変える
- アスペクト比: 自由 / 1:1 / 4:3 / 3:2 / 16:9 / 指定（W:H）。「縦横を入れ替え」で 3:4 など縦向きに
- 複数選択していれば ◀ ▶（PageUp / PageDown）で切り替え。Enter で「保存して次へ」
- 「全部を同じ比で中央から切り抜き」で選択中の全部を一括で
- 保存先は元と同じフォルダ（既定）か指定のフォルダ。名前は `元の名前_crop`（同名があれば ` (2)` などを付けて上書きしない）

### 連結（Ctrl+M、2 枚以上）

| 項目 | 内容 |
|---|---|
| 並べ方 | 横 / 縦 / グリッド（列の数） |
| 大きさ | 横は高さ・縦は幅を そのまま / 小さい方 / 大きい方 / 指定 px に揃える。グリッドは 元の一番大きい画像 / セルを指定 px 角に |
| 揃え | 横は上 / 中央 / 下、縦は左 / 中央 / 右 |
| 間隔・外の余白 | px で指定 |
| 背景 | 色（色の選択画面あり）/ 透明（JPG では白） |
| 形式 | PNG / JPG / WEBP |

- 設定を変えるとすぐプレビューに反映する。プレビューは縮小して読んだ画像（長辺 1200px まで・合計 1600 万画素まで）で組み、長辺 2400px のキャンバスに直接描くので、何百枚選んでもメモリは増えすぎない。原寸で読むのは保存するときだけ
- 仕上がりが形式の上限（WEBP 16383px・JPG 65535px）を超えるときは保存前に知らせ、とても大きいとき（2 億画素超）は確認する

## リリースの手順

GitHub でリリースを公開すると、GitHub Actions（`.github/workflows/release.yml`）がテストしてから
`ImageViewer.exe`（単一ファイル・インストール不要）を作り、そのリリースに添付する（数分かかる）。

1. main に変更を入れる
2. リリースを作る（タグ `v0.1.4` のように。説明文も書く）
   - `gh release create v0.1.4 --target main --title v0.1.4 --notes "…"`、または GitHub の Releases 画面から
3. Actions の「リリース」が終わると、リリースに `ImageViewer.exe` とチェックサム `ImageViewer.exe.sha256`（アプリの自動更新が使う）が付く。テストが通らなければ付かない（Actions で理由を確認）

バージョン番号はタグから付く（`v0.1.4` → 0.1.4）。手元で作るときは次のとおり（出力は `publish` フォルダ）:

```
dotnet publish src/ImageViewer.App/ImageViewer.App.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -p:Version=0.1.4 -o publish
```
